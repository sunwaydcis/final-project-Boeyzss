# Runtime data

The application creates `microfinance-state.json` here after the first successful change. The runtime file is ignored by Git so personal or demonstration records are not accidentally submitted.

For a fictional demo portfolio, copy `src/main/resources/sample-state.json` to this directory and rename it `microfinance-state.json`.
